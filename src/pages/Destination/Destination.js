import classes from "./Destination.module.css";

const Destination = () => {
  return (
    <main className={classes.destination}>
      <h1> Desination page</h1>
    </main>
  );
};

export default Destination;
